#include "LinkList.h"
#include <cstring> 
#include <conio.h>

/*
********************READ_ME************************
窗口设置价格的时候请设置五块钱，不然后面查询窗口有点麻烦，因为它好像是把窗口类的每一个数据都比较，有一个数据不一样都查不到
删除窗口链表 的 每个窗口节点 的 学生链表 的 头节点 未完成（模拟时间流动学生从当前窗口离开）
*/

int main()
{
	void CanteenTest();
	CanteenTest();


	return 0;
}